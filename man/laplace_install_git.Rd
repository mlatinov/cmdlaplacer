% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace_git_install.R
\name{laplace_install_git}
\alias{laplace_install_git}
\title{Add a git-hosted Laplace library as a dependency}
\usage{
laplace_install_git(
  name,
  repo,
  tag = NULL,
  rev = NULL,
  subdir = NULL,
  project_dir = "."
)
}
\arguments{
\item{name}{Character. The package name to register the dependency under
(as it will be used in \code{pkg::func()} calls and in \code{laplace.toml}).}

\item{repo}{Character. Git URL of the repository, e.g.
\code{"https://github.com/user/some-laplace-lib"}.}

\item{tag}{Character or \code{NULL}. Git tag to pin to (e.g. \code{"0.1.0"}). Give
exactly one of \code{tag} or \code{rev}.}

\item{rev}{Character or \code{NULL}. Git commit SHA to pin to instead of a tag.
Give exactly one of \code{tag} or \code{rev}.}

\item{subdir}{Character or \code{NULL}. Subdirectory within the repository
where the library's \code{laplace.toml} lives, if it isn't at the repo root.}

\item{project_dir}{Character. Directory containing the \code{laplace.toml} of
the \emph{project} you're adding this dependency to (not the dependency's
own manifest). Defaults to the current working directory.}
}
\value{
Invisibly, the character vector of combined stdout/stderr lines
from \verb{laplace add}.
}
\description{
Thin wrapper around \verb{laplace add --git}, for adding a \code{.laplacelib}
library dependency straight from a git repository without hand-assembling
the CLI flags each time. Requires exactly one of \code{tag} or \code{rev} to pin the
dependency, matching what \verb{laplace add --git} itself requires.
}
\details{
This function requires the \code{laplace} CLI to be installed and available on
\code{PATH} (see \code{\link[=laplace_install]{laplace_install()}}) and \code{project_dir} to contain a
\code{laplace.toml} for the project you're adding the dependency to — this
function does not create one.
}
\examples{
\dontrun{
laplace_install_git(
  "transformations",
  "https://github.com/mlatinov/laplace-transform",
  tag = "0.1.0",
  subdir = "laplace"
)

# Pin to a commit instead of a tag
laplace_install_git(
  "transformations",
  "https://github.com/mlatinov/laplace-transform",
  rev = "a35b3f2"
)
}

}
