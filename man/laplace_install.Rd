% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/laplace_installer.R
\name{laplace_install}
\alias{laplace_install}
\title{Install the Laplace CLI}
\usage{
laplace_install(
  root = "~/.local",
  repo = "https://github.com/mlatinov/laplace",
  ref = NULL,
  quiet = FALSE
)
}
\arguments{
\item{root}{Character. Install root passed to \verb{cargo install --root}.
The binary ends up at \code{file.path(root, "bin", "laplace")}. Defaults to
\code{"~/.local"}, matching the upstream documentation.}

\item{repo}{Character. Git URL to clone. Defaults to the main Laplace
repository.}

\item{ref}{Character or \code{NULL}. Optional git branch or tag to check out
(passed to \verb{git clone --branch}). \code{NULL} (the default) uses the
repository's default branch.}

\item{quiet}{Logical. If \code{TRUE}, suppress progress messages. Errors are
always raised regardless of this setting. Default \code{FALSE}.}
}
\value{
Invisibly, the path to the installed \code{laplace} binary.
}
\description{
Builds and installs the \code{laplace} command-line compiler from source,
using \code{git} to fetch it and \code{cargo} to build it — mirroring the manual
installation steps documented at
\url{https://github.com/mlatinov/laplace#installing-laplace}. This is a
convenience wrapper only: it does not vendor or patch anything, and the
resulting binary is exactly what you'd get by running the same \code{git}
and \code{cargo} commands yourself.
}
\details{
\code{laplace_model()} will offer to call this function automatically in
interactive sessions when the \code{laplace} CLI can't be found. You can also
call it directly.

Requires \code{git} and a stable Rust toolchain (\code{cargo}) to already be
installed and on \code{PATH}; this function does not install either of those
for you. Building \code{laplace} itself does not require Stan or \code{stanc} —
those are only needed for the optional \code{--validate} flag on
\verb{laplace build}.

If the install directory's \verb{bin/} is not already on \code{PATH}, it is added
to \code{PATH} for the current R session only (via \code{Sys.setenv()}), and a
message explains how to make that permanent in your shell profile.

\verb{cargo install} is always called with \code{--force}. Without it, Cargo
silently skips reinstalling when it thinks the same version is already
present, which would leave a stale binary in place if the source changed
without a version bump in \code{Cargo.toml} — exactly the kind of thing a
"reinstall" helper should not do quietly.
}
\examples{
\dontrun{
laplace_install()

# Install a specific tag into a custom location
laplace_install(root = "~/.laplace", ref = "v0.3.0")
}

}
